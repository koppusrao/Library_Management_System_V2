// ======================================================
// frontend/src/api/api.js
// Centralized API layer (SAFE & STABLE)
// ======================================================

import axios from "axios";

const BASE_URL = "http://localhost:3000"; // Gateway URL

// ================= BOOK APIs =================

export async function listBooks() {
  const res = await axios.get(`${BASE_URL}/books`);
  return res.data;
}

export async function createBook(book) {
  const res = await axios.post(`${BASE_URL}/books`, { book });
  return res.data;
}

export async function updateBook(id, book) {
  const res = await axios.put(`${BASE_URL}/books/${id}`, { book });
  return res.data;
}

export async function deleteBook(id) {
  const res = await axios.delete(`${BASE_URL}/books/${id}`);
  return res.data;
}

// ================= MEMBER APIs =================

export async function listMembers() {
  const res = await axios.get(`${BASE_URL}/members`);
  return res.data;
}

export async function createMember(member) {
  const res = await axios.post(`${BASE_URL}/members`, { member });
  return res.data;
}

export async function updateMember(id, member) {
  const res = await axios.put(`${BASE_URL}/members/${id}`, { member });
  return res.data;
}

export async function deleteMember(id) {
  const res = await axios.delete(`${BASE_URL}/members/${id}`);
  return res.data;
}